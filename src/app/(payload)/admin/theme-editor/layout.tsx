import '@/components/ThemeEditor/theme-editor.scss'

export const metadata = {
  title: 'Theme Editor | Veya',
}

export default function ThemeEditorLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
